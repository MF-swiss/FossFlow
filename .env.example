# Server Storage Configuration
# Set to true to enable server-side storage for multi-device access
# Default is true - diagrams are saved to ./diagrams directory on the host
ENABLE_SERVER_STORAGE=true

# Git backup (optional)
# Set to true to automatically commit changes to git
ENABLE_GIT_BACKUP=false

# Storage path (inside container)
# This is mapped to ./diagrams on the host via Docker volume
STORAGE_PATH=/data/diagrams

# Backend port (usually doesn't need changing)
BACKEND_PORT=3001

# CORS allowlist for backend API (comma-separated origins)
# SECURITY RECOMMENDATION: do NOT leave empty in production.
# Set explicit origins only (example below supports local app + one prod host).
# Example: CORS_ORIGINS=http://localhost:3000,https://fossflow.example.com
CORS_ORIGINS=http://localhost:3000

# API rate limiting
# Window is in milliseconds (default 15 minutes)
RATE_LIMIT_WINDOW_MS=900000
# Max API requests per window per IP
RATE_LIMIT_MAX=300

# HTTP Basic Auth (optional)
# setup both USER and PASSWORD to enable authentication
HTTP_AUTH_USER=
HTTP_AUTH_PASSWORD=
